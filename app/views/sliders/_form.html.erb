<%= form_for(@slider) do |f| %>
  <% if @slider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slider.errors.count, "error") %> prohibited this slider from being saved:</h2>

      <ul>
      <% @slider.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="field">
    <%= f.label :showstartdate %><br>
    <%= f.datetime_select :showstartdate %>
  </div>
  <div class="field">
    <%= f.label :showenddate %><br>
    <%= f.datetime_select :showenddate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
